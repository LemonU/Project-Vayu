package cas.sorting;

import java.util.ArrayList;
import cas.vayu.DisasterPoint;
import cas.vayu.WeatherTypeEnum;

public class Filter {
	
	
	public ArrayList<DisasterPoint> filter(ArrayList<DisasterPoint> list, WeatherTypeEnum disaster, boolean lookFor) {
		
	}
	
	public Filter(WeatherTypeEnum input, boolean look) {
		this.lookFor = look;
		this.disaster = input;
	}
}
